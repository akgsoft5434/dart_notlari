# 🐹 Dart Konuları (Kolaydan Zora)

Bu liste, Dart dilini sıfırdan öğrenmek isteyenler için **kolaydan zora** doğru hazırlanmıştır.  
Flutter öğrenmeden önce Dart temellerini sağlamlaştırmak için idealdir.

---

## 🟢 Temel Seviye (Başlangıç)

1. **Dart Giriş ve Kurulum**
   - Dart SDK nedir, nasıl kurulur  
   - DartPad kullanımı  
   - `main()` fonksiyonu  

2. **Değişkenler ve Veri Tipleri**
   - `int`, `double`, `String`, `bool`, `var`, `dynamic`  
   - `final` ve `const` farkı  

3. **Yorum Satırları ve Yazdırma**
   - `//`, `/* ... */`  
   - `print()` fonksiyonu  

4. **Operatörler**
   - Aritmetik (`+`, `-`, `*`, `/`, `%`)  
   - Karşılaştırma (`==`, `!=`, `>`, `<`)  
   - Mantıksal (`&&`, `||`, `!`)  

5. **Koşul İfadeleri**
   - `if`, `else if`, `else`  
   - `switch` – `case`  

6. **Döngüler**
   - `for`, `while`, `do-while`  
   - `break`, `continue`  

7. **Listeler (Lists)**
   - Liste tanımlama, eleman ekleme, silme  
   - `forEach`, `map`, `where`, `contains`  

8. **Set ve Map (Koleksiyonlar)**
   - `Set` yapısı (benzersiz elemanlar)  
   - `Map` (anahtar–değer çiftleri)  

---

## 🟡 Orta Seviye

9. **Fonksiyonlar**
   - Parametreler, dönüş tipleri  
   - İsimlendirilmiş (named) ve isteğe bağlı (optional) parametreler  
   - Lambda (arrow) fonksiyonlar  

10. **Null Safety**
    - `?`, `!`, `??`, `??=` operatörleri  
    - Null kontrolü mantığı  

11. **Sınıflar (Classes) ve Nesneler (Objects)**
    - `class`, `this`, `constructor`  
    - Örnek oluşturma, özellik ve metod tanımlama  

12. **Encapsulation (Kapsülleme)**
    - `_private` üyeler  
    - Getter ve Setter kullanımı  

13. **Inheritance (Kalıtım)**
    - `extends`, `super`  
    - Metot geçersiz kılma (`@override`)  

14. **Polymorphism (Çok Biçimlilik)**
    - Üst sınıf referansı ile alt sınıf nesnesi kullanımı  
    - `abstract` sınıflar ve `interface`  

15. **Mixins**
    - `with` anahtar kelimesiyle özellik paylaşımı  

16. **Enum (Sabit Gruplar)**
    - Enum tanımı ve kullanımı  

17. **Generics (Tür Parametreleri)**
    - `List<T>`, `Map<K, V>` gibi kullanımlar  
    - Generic fonksiyonlar  

18. **Error Handling (Hata Yönetimi)**
    - `try`, `catch`, `finally`, `throw`  
    - Özel hata sınıfları  

---

## 🔵 İleri Seviye

19. **Asenkron Programlama**
    - `Future`, `async`, `await`  
    - `Stream` yapısı ve `yield` kullanımı  

20. **Extension Methods**
    - Mevcut sınıflara yeni metot ekleme  

21. **Isolate ve Paralel Programlama**
    - Çoklu iş parçacığı (thread) mantığı  
    - `compute()` ve `Isolate.spawn()`  

22. **Late Initialization**
    - `late` anahtar kelimesi ve kullanım senaryoları  

23. **Cascade Notation (`..`)**
    - Zincirleme işlemler  

24. **Record Types (Dart 3 ile)**
    - `(int, String)` yapıları  
    - Destructuring (parçalama)  

25. **Pattern Matching (Dart 3+)**
    - `switch` ve `if` içinde desen eşleştirme  

26. **Sealed Class ve Interface (Dart 3+)**
    - Kapsamlı tip güvenliği için `sealed`, `interface`, `base`, `final`  

27. **Packages ve Libraries**
    - `import`, `export`  
    - Paket oluşturma ve pub.dev kullanımı  

28. **Unit Test ve Integration Test**
    - `test` paketi ile test yazma  
    - Mock ve async testler  

29. **JSON İşlemleri**
    - `dart:convert` (`jsonEncode`, `jsonDecode`)  
    - Model sınıfları oluşturma  

30. **File I/O ve HTTP İşlemleri**
    - `dart:io`, `http` paketi kullanımı  

---

## 🔴 Uzman Seviye

31. **Dart VM ve AOT/JIT Derleme**
    - Performans farkları, build modları  

32. **Reflection ve Metadata**
    - `dart:mirrors` ile yansıma (reflection)  

33. **Custom Annotations**
    - Özel açıklamalar (metadata) oluşturma  

34. **Code Generation (build_runner)**
    - `json_serializable`, `freezed`, `hive_generator` gibi araçlar  

35. **Concurrency Patterns**
    - Event loop, microtask queue mantığı  

---

